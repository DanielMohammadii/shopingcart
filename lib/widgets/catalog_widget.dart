import 'package:flutter/material.dart';

class CatalogWidet extends StatelessWidget {
  const CatalogWidet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
